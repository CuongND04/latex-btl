\documentclass[../BTL_PTIT.tex]{subfiles}
\begin{document}
Chương này mô tả nội dung chính của đề tài theo đúng tiến trình triển khai một hệ thống nhúng hoàn chỉnh. Từ các yêu cầu đầu bài, nhóm có thể xác định bài toán thực tế, yêu cầu hệ thống, phần cứng cần dùng, chức năng phần mềm phải phát triển, cách tích hợp các khối chức năng và cách đánh giá kết quả cuối cùng. Với một đề tài mẫu như hệ thống giám sát và xử lý khí độc thông minh, cấu trúc chương này giúp người đọc theo dõi được toàn bộ vòng đời xây dựng hệ thống từ thiết kế đến kiểm thử.

\section{Xác định bài toán và yêu cầu hệ thống}

\subsection{Mô tả bài toán thực tế}
Bối cảnh triển khai của hệ thống là các môi trường công nghiệp như xưởng sản xuất hóa chất, khu vực sơn phủ hoặc các phòng chứa dung môi, nơi rò rỉ khí độc như Ammonia, Benzene hoặc VOC có thể gây ngộ độc cấp tính và cháy nổ. Việc giám sát thủ công thường không bảo đảm độ phủ và tính liên tục, dẫn đến phản ứng chậm khi nồng độ khí vượt ngưỡng an toàn.

Giải pháp được đề xuất là xây dựng một hệ thống giám sát và xử lý khí độc thông minh sử dụng vi điều khiển ESP32. Hệ thống không chỉ quan trắc nồng độ khí theo thời gian thực mà còn thực hiện tác vụ phản hồi bằng cách tự động kích hoạt quạt thông gió để lưu thông không khí khi phát hiện ô nhiễm. Dữ liệu được quản lý tập trung qua Cloud để phân tích xu hướng và phục vụ báo cáo an toàn lao động.

\subsection{Yêu cầu chức năng}
Hệ thống tập trung vào khả năng phát hiện khí độc và phản ứng nhanh.

\subsubsection{Thu thập dữ liệu nồng độ khí}
\begin{itemize}
    \item Phát hiện và đo lường nồng độ các loại khí độc hại như Amoniac, Sulfide, hơi Benzen và VOC bằng cảm biến chất lượng không khí MQ135.
    \item Chuyển đổi tín hiệu analog sang giá trị nồng độ ppm để xử lý.
\end{itemize}

\subsubsection{Xử lý và điều khiển thiết bị (Actuation Control)}
\begin{itemize}
    \item \textbf{Chế độ Tự động (AUTO):} Hệ thống tự động kích hoạt quạt thông gió và còi báo động ngay khi nồng độ khí đo được vượt ngưỡng cài đặt an toàn.
    \item \textbf{Chế độ Thủ công (MANUAL):} Người quản lý có thể điều khiển cưỡng bức trạng thái quạt (Bật/Tắt) trực tiếp từ Dashboard thông qua giao thức MQTT.
\end{itemize}

\subsubsection{Truyền thông và giám sát}
\begin{itemize}
    \item Gửi gói tin dữ liệu định dạng JSON lên HiveMQ Broker thông qua giao thức MQTT.
    \item Hiển thị trạng thái quạt và biểu đồ nồng độ khí trên Web Dashboard thời gian thực.
\end{itemize}

\subsubsection{Quản trị nâng cao}
\begin{itemize}
    \item \textbf{Cập nhật OTA:} Hỗ trợ cập nhật firmware từ xa cho ESP32 để hiệu chỉnh cảm biến hoặc thay đổi logic điều khiển mà không cần can thiệp phần cứng.
    \item \textbf{AI Summary (định hướng):} Chức năng tóm tắt xu hướng ô nhiễm theo giờ bằng AI đã được đề xuất nhưng chưa triển khai trong phiên bản hiện tại.
\end{itemize}

\subsection{Yêu cầu phi chức năng}

\subsubsection{Tính thời gian thực (Real-time)}
\begin{itemize}
    \item Độ trễ dữ liệu từ cảm biến MQ135 đến Dashboard không quá 3 giây.
    \item Thời gian phản hồi kích hoạt quạt khi phát hiện rò rỉ khí phải dưới 5 giây.
\end{itemize}

\subsubsection{Quản lý năng lượng (Power Management)}
\begin{itemize}
    \item Sử dụng nguồn chính từ Adapter 5V-2A.
    \item Tích hợp hệ thống pin dự phòng Lithium 18650 và mạch chuyển nguồn tự động YX850 (UPS) để duy trì hoạt động giám sát khi mất điện lưới.
    \item Áp dụng chế độ Sleep Mode giữa các chu kỳ truyền tin để tối ưu điện năng khi chạy pin.
\end{itemize}

\subsubsection{Độ tin cậy và bảo mật}
\begin{itemize}[label=\textbullet]
    \item Đảm bảo tỷ lệ truyền gói tin thành công đạt ít nhất 98\% trong môi trường công nghiệp.
    \item Xác thực thiết bị và người dùng qua mã thông báo JWT; mã hóa dữ liệu truyền tải qua TLS/SSL.
\end{itemize}

\subsubsection{Chi phí (Cost)}
Tổng chi phí đầu tư phần cứng nếu triển khai thực tế với số lượng lớn, ví dụ 20 trạm cảm biến, phải dưới 250 triệu VNĐ.

\subsection{Phân tích ràng buộc}

\subsubsection{Tài nguyên thiết bị (Hardware Limits)}
\begin{itemize}[label=\textbullet]
    \item CPU/RAM: ESP32 có dung lượng SRAM giới hạn (520 KB), các tác vụ phân tích dữ liệu lớn hoặc AI phải được đẩy lên Cloud xử lý.
    \item Lập trình đa nhiệm: Sử dụng FreeRTOS để phân chia các Task (Sensor Task, MQTT Task, Control Task) với mức ưu tiên khác nhau, tránh xung đột khi điều khiển quạt.
\end{itemize}

\subsubsection{Ràng buộc cảm biến}
\begin{itemize}[label=\textbullet]
    \item Cảm biến MQ135 cần thời gian sấy (Warm-up) để bộ phận nhiệt đạt đủ nhiệt độ đo chính xác, yêu cầu nguồn cấp ổn định 5V.
\end{itemize}

\subsubsection{Môi trường hoạt động}
\begin{itemize}[label=\textbullet]
    \item Thiết bị phải vận hành ổn định trong dải nhiệt độ công nghiệp từ 0°C đến 70°C.
    \item Vỏ hộp thiết bị đạt tiêu chuẩn IP65 để bảo vệ mạch điện khỏi hơi dung môi và bụi bẩn trong nhà máy.
\end{itemize}

\section{Phần cứng của hệ thống}
\subsection{Block diagram}
\begin{figure}[H]
\centering
\includegraphics[width=0.95\linewidth]{Hinh_ve/block_diagram.png}
\caption{Block diagram của hệ thống}
\label{fig:block_diagram_he_thong}
\end{figure}

\subsection{Vi điều khiển ESP32}
ESP32-WROOM-32D là một module Wi-Fi + Bluetooth tích hợp, phát triển bởi Espressif Systems, được sử dụng rộng rãi trong IoT (Internet of Things), tự động hóa và các thiết bị nhúng thông minh nhờ khả năng xử lý mạnh mẽ và kết nối không dây. Trên thực tế, người ta thường sử dụng bo mạch phát triển ESP32 DevKit V1 tích hợp sẵn module ESP32-WROOM-32D để dễ dàng lập trình và kết nối.

\begin{figure}[H]
\centering
\includegraphics[width=0.75\linewidth]{Hinh_ve/esp32.png}
\caption{Bo mạch ESP32 DevKit V1}
\label{fig:esp32_devkit_v1}
\end{figure}

ESP32 được lựa chọn thay vì Arduino vì các lý do sau:
\begin{itemize}[label=\textbullet]
    \item Tích hợp sẵn WiFi, phù hợp hệ thống IoT sử dụng MQTT.
    \item Hiệu năng cao, đáp ứng xử lý dữ liệu và truyền thông đồng thời.
    \item Bộ nhớ lớn, hỗ trợ tốt các giao thức mạng.
    \item Giảm độ phức tạp phần cứng (không cần module WiFi ngoài).
    \item Dễ mở rộng và phát triển trong tương lai.
\end{itemize}

Trong khi đó, Arduino không phù hợp do:
\begin{itemize}[label=\textbullet]
    \item Không có WiFi tích hợp.
    \item Hiệu năng thấp.
    \item Hạn chế bộ nhớ.
    \item Khó triển khai hệ thống IoT hoàn chỉnh.
\end{itemize}

Các thông số chính:
\begin{itemize}[label=\textbullet]
    \item CPU: Xtensa dual-core 32-bit LX6 (tốc độ tối đa 240 MHz).
    \item Bộ nhớ: 448 KB ROM, 520 KB SRAM, 4 MB Flash (trên module).
    \item Kết nối: Wi-Fi 802.11 b/g/n, Bluetooth v4.2 (Classic + BLE).
    \item GPIO: 34 chân I/O, hỗ trợ PWM, ADC (12-bit), DAC (8-bit), SPI, I2C, UART.
    \item Điện áp hoạt động: 3.0V-3.6V (DevKit hỗ trợ cấp từ 5V qua cổng USB).
\end{itemize}

Các linh kiện chính trên bo mạch ESP32 DevKit 32D:
\begin{itemize}[label=\textbullet]
    \item \textbf{Module ESP32-WROOM-32D:}
    \begin{itemize}
        \item Thành phần trung tâm (SoC ESP32 + Flash 4 MB).
        \item Chứa bộ xử lý, bộ nhớ, WiFi/Bluetooth và mạch RF.
    \end{itemize}
    \item \textbf{USB-to-UART Bridge (CP2102 hoặc CH340):}
    \begin{itemize}
        \item Cho phép nạp chương trình và giao tiếp với máy tính thông qua cổng USB.
        \item Chuyển đổi tín hiệu USB sang UART.
    \end{itemize}
    \item \textbf{Ổn áp AMS1117-3.3V:} Giúp chuyển đổi điện áp từ 5V (USB) hoặc Vin xuống 3.3V để nuôi ESP32.
    \item \textbf{Cổng Micro-USB:} Dùng để cấp nguồn (5V) và nạp code.
    \item \textbf{Nút nhấn:}
    \begin{itemize}
        \item EN (Reset): Reset lại vi điều khiển.
        \item BOOT (IO0): Giữ để đưa ESP32 vào chế độ nạp chương trình.
    \end{itemize}
    \item \textbf{Thạch anh (Crystal 40 MHz):} Tạo xung clock ổn định cho chip ESP32.
    \item \textbf{LED chỉ thị (thường nối với GPIO2):} Báo hiệu nguồn hoặc được dùng test xuất tín hiệu.
    \item \textbf{Hàng chân header (Male header pins):} Xuất các chân GPIO, nguồn (3.3V, 5V), GND để dễ dàng kết nối với breadboard hoặc module khác.
    \item \textbf{Các tụ điện, điện trở dán (SMD):} Dùng để lọc nhiễu, ổn định nguồn, kéo lên hoặc kéo xuống cho các chân tín hiệu.
\end{itemize}

\subsection{MQ-135}
MQ-135 là cảm biến khí bán dẫn sử dụng vật liệu thiếc dioxide. Nó được thiết kế đặc biệt để phát hiện nhiều loại khí độc hại và khí gây ô nhiễm môi trường. Do độ nhạy cao và giá thành rẻ, nó là lựa chọn hàng đầu cho các ứng dụng Air Quality Monitor (Giám sát chất lượng không khí) trong nhà và văn phòng.

\begin{figure}[H]
\centering
\includegraphics[width=0.42\linewidth]{Hinh_ve/mq135_1.png}
\caption{Cảm biến khí MQ-135}
\label{fig:mq135_sensor}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.75\linewidth]{Hinh_ve/mq2.png}
\caption{Sơ đồ chân và giao tiếp module cảm biến khí}
\label{fig:mq_module_pinout}
\end{figure}

Các loại khí phát hiện (Detection Range):
\begin{itemize}
    \item Amoniac: Rất nhạy.
    \item Sulfide và hơi Benzen: Nhạy.
    \item Hơi rượu (Alcohol), khói (Smoke): Nhạy trung bình.
    \item Các oxit nitơ: Phát hiện được.
    \item CO2: Có khả năng phát hiện nhưng không chuyên dụng (thường dùng để ước lượng mức độ ô nhiễm không khí nói chung).
\end{itemize}

Thông số kỹ thuật (Technical Specifications):
\begin{itemize}
    \item Điện áp hoạt động (VCC): 5V DC $\pm$ 0.1V (lưu ý: ESP32 dùng logic 3.3V nhưng chân VCC của cảm biến phải nối 5V để bộ phận sấy hoạt động đủ nhiệt).
    \item Điện áp mạch sấy (Heater Voltage): 5V DC/AC.
    \item Công suất tiêu thụ: Khoảng 800mW (cảm biến sẽ khá nóng khi hoạt động, đây là bình thường).
    \item Đầu ra dữ liệu:
    \begin{itemize}
        \item Analog (A0): Điện áp biến thiên từ 0 - 5V (tương ứng với nồng độ khí).
        \item Digital (D0): Tín hiệu 0 hoặc 1 (được so sánh qua Op-Amp LM393 tích hợp trên module, chỉnh ngưỡng bằng biến trở).
    \end{itemize}
    \item Phạm vi phát hiện: 10ppm - 1000ppm (tùy loại khí).
\end{itemize}

\subsection{Quạt tản nhiệt 5V mini 4x4}
Quạt tản nhiệt nhỏ gọn, sử dụng nguồn 5V phù hợp cho hệ thống.

\begin{figure}[H]
\centering
\includegraphics[width=0.45\linewidth]{Hinh_ve/quat_tan_nhiet.png}
\caption{Quạt tản nhiệt mini 5V kích thước 4x4}
\label{fig:quat_tan_nhiet_5v}
\end{figure}

\subsection{Module LCD I2C 16x2}
Module LCD I2C 16x2 là một màn hình ký tự 16 cột × 2 hàng kết hợp với mô-đun chuyển đổi I2C PCF8574. Thiết bị này cho phép hiển thị thông tin tại chỗ (offline display) mà không cần phụ thuộc vào kết nối internet hoặc điện thoại di động.

Trong hệ thống này, LCD phục vụ mục đích quan trọng:
\begin{itemize}
    \item Hiển thị thông tin nồng độ khí đo được từ cảm biến MQ-135 (giá trị raw ADC).
    \item Hiển thị mức độ chất lượng không khí (GOOD, MOD, BAD, DANG).
    \item Hiển thị trạng thái quạt (ON/OFF) và chế độ hoạt động (AUTO/MANUAL) của từng phòng.
    \item Giúp vận hành viên hoặc kỹ sư bảo trì đứng tại trạm có thể kiểm tra tình trạng hệ thống ngay lập tức mà không cần mở điện thoại hay laptop.
\end{itemize}

Thông số kỹ thuật:
\begin{itemize}[label=\textbullet]
    \item Loại màn hình: LCD ký tự 16×2 (16 cột, 2 hàng).
    \item Giao diện: I2C (2 dây: SDA, SCL), sử dụng chip PCF8574 làm bộ chuyển đổi.
    \item Điều chỉ độ sáng: Có biến trở trên module.
    \item Điện áp hoạt động: 5V DC.
    \item Dòng điện: Khoảng 50-100 mA (tùy độ sáng).
\end{itemize}

Cấu hình trong dự án:
\begin{itemize}[label=\textbullet]
    \item Địa chỉ I2C mặc định: 0x27 (có thể khác tùy module).
    \item Chân SDA: GPIO 21 của ESP32.
    \item Chân SCL: GPIO 22 của ESP32.
    \item Thư viện sử dụng: \texttt{LiquidCrystal\_I2C}.
\end{itemize}

Hoạt động:
\begin{itemize}[label=\textbullet]
    \item Trong task LCD, hệ thống xoay vòng (paging) hiển thị thông tin của hai phòng mỗi 2 giây.
    \item Mỗi trang hiển thị khoảng 2 dòng với các thông tin: nồng độ khí, mức cảnh báo, trạng thái quạt, chế độ điều khiển.
    \item Khi có sự kiện chuyển trạng thái quạt (relay đóng/ngắt), task LCD sẽ re-initialize I2C và LCD để chống lại nhiễu đường bus I2C gây bởi dòng cảm ứng ngược từ cuộn relay.
\end{itemize}

\begin{figure}[H]
\centering
\includegraphics[width=0.5\linewidth]{Hinh_ve/lcd_i2c_16x2.png}
\caption{Module LCD I2C 16×2 PCF8574}
\label{fig:lcd_i2c_16x2_module}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.6\linewidth]{Hinh_ve/lcd_running.png}
\caption{Màn hình LCD đang hoạt động hiển thị nồng độ khí và trạng thái quạt}
\label{fig:lcd_running_display}
\end{figure}

\subsection{Module Relay 2 kênh 5V}
Module Relay 2 kênh là thiết bị chuyển mạch điện tử dùng để điều khiển các tải điện (như quạt, mô-tơ, đèn) hoạt động ở mức công suất cao từ các tín hiệu điều khiển mức thấp (từ GPIO của ESP32).

Vai trò trong hệ thống:
\begin{itemize}[label=\textbullet]
    \item Kênh 1: Điều khiển quạt của phòng 1 (bật/tắt).
    \item Kênh 2: Điều khiển quạt của phòng 2 (bật/tắt).
    \item Khi nồng độ khí vượt ngưỡng (chế độ AUTO) hoặc khi nhận lệnh từ người dùng (chế độ MANUAL), GPIO của ESP32 sẽ kích hoạt relay để cấp điện cho quạt.
\end{itemize}

Thông số kỹ thuật:
\begin{itemize}[label=\textbullet]
    \item Số kênh: 2 (hai relay độc lập).
    \item Cuộn dây: 5V DC.
    \item Tiếp điểm chính: Thường có khả năng chịu tải tới 10A / 250V AC hoặc 10A / 30V DC (tùy loại).
    \item Loại kích hoạt: Active LOW (cuộn dây được kích hoạt khi tín hiệu vào là mức thấp 0V).
    \item Cách ly: Sử dụng opto-coupler (opto-cách ly) để tách biệt mạch điều khiển (3.3V từ GPIO ESP32) khỏi mạch công suất (5V/240V).
    \item Dòng điều khiển: Khoảng 20-30 mA mỗi kênh.
\end{itemize}

Cấu hình trong dự án:
\begin{itemize}[label=\textbullet]
    \item Chân IN1 (Input kênh 1): GPIO 32 của ESP32 $\rightarrow$ Quạt phòng 1.
    \item Chân IN2 (Input kênh 2): GPIO 33 của ESP32 $\rightarrow$ Quạt phòng 2.
    \item Chân GND: Nối chung đất với ESP32 và nguồn 5V.
    \item Chân VCC: Cấp 5V từ hệ thống nguồn.
    \item Khởi tạo trong \texttt{setup()}: \texttt{pinMode(32, OUTPUT); pinMode(33, OUTPUT);} và đặt trạng thái ban đầu an toàn trước khi vào vòng điều khiển.
\end{itemize}

Hoạt động:
\begin{itemize}[label=\textbullet]
    \item ESP32 xuất tín hiệu điều khiển trên GPIO 32 và GPIO 33 để bật/tắt relay tương ứng từng phòng.
    \item Mức kích hoạt thực tế (HIGH hoặc LOW) phụ thuộc loại module relay, được chuẩn hóa trong hàm điều khiển của firmware.
\end{itemize}

\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{Hinh_ve/relay_2_kenh.png}
\caption{Module Relay 2 kênh 5V với sơ đồ chân kết nối}
\label{fig:relay_2_kenh_module}
\end{figure}

\subsection{Servo SG90}

\subsubsection{Giới thiệu nhanh}
Servo SG90 là loại động cơ servo siêu nhỏ (Micro Servo), cực kỳ phổ biến trong cộng đồng Maker và STEM. Nó nổi tiếng nhờ sự cân bằng giữa giá thành rẻ, kích thước nhỏ (khoảng 9 g) và khả năng điều khiển góc quay chính xác. Thường dùng với Arduino, ESP32 hoặc Raspberry Pi.

\begin{figure}[H]
\centering
\includegraphics[width=0.5\linewidth]{Hinh_ve/servo_sg90.png}
\caption{Servo SG90 (Micro Servo)}
\label{fig:servo_sg90}
\end{figure}

\subsubsection{Chức năng \& Ứng dụng}
Khác với động cơ DC quay liên tục, Servo SG90 được thiết kế để duy trì một góc quay xác định (thường $0^\circ$ đến $180^\circ$).
\begin{itemize}[label=\textbullet]
    \item \textbf{Chức năng:} Điều khiển vị trí góc của trục động cơ dựa trên tín hiệu xung PWM.
    \item \textbf{Ứng dụng:}
    \begin{itemize}
        \item Khớp nối trong cánh tay robot (Robot Arm).
        \item Điều khiển góc lái cho xe điều khiển từ xa (RC Car) hoặc máy bay mô hình.
        \item Đóng/mở khóa cửa thông minh hoặc nắp thùng rác tự động.
        \item Điều khiển góc quét cho cảm biến siêu âm tránh vật cản.
    \end{itemize}
\end{itemize}

\subsubsection{Thông số kỹ thuật chính}
\begin{tabular}{lp{8.5cm}}
\textbf{Trọng lượng} & 9 g \\
\textbf{Kích thước} & 22.2 $\times$ 11.8 $\times$ 22.7 mm \\
\textbf{Điện áp hoạt động} & 4.8 V -- 6 V (ổn định nhất ở 5 V) \\
\textbf{Lực kéo (Stall Torque)} & 1.2 kg/cm (tại 4.8 V) đến 1.6 kg/cm (tại 6 V) \\
\textbf{Tốc độ phản ứng} & 0.1 s / $60^\circ$ \\
\textbf{Góc quay} & $0^\circ$ -- $180^\circ$ \\
\textbf{Chất liệu bánh răng} & Nhựa (POM)
\end{tabular}

\subsubsection{Sơ đồ chân (Pinout)}
Servo SG90 có 3 dây màu chuẩn:
\begin{itemize}
    \item Dây Nâu (Brown): GND.
    \item Dây Đỏ (Red): VCC (5 V).
    \item Dây Cam (Orange): Tín hiệu điều khiển (PWM Signal).
\end{itemize}

\subsubsection{Nguyên lý hoạt động}
Nguyên lý cốt lõi là Điều chế độ rộng xung (PWM). Bên trong servo có mạch điều khiển và biến trở gắn với trục đầu ra. Mạch so sánh vị trí hiện tại với độ rộng xung nhận được.
\begin{itemize}[label=\textbullet]
    \item Tần số hoạt động: thường 50 Hz (chu kỳ 20 ms).
    \item Độ rộng xung (High) quyết định góc:
    \begin{itemize}
        \item 0.5 ms $\rightarrow$ $0^\circ$.
        \item 1.5 ms $\rightarrow$ $90^\circ$.
        \item 2.5 ms $\rightarrow$ $180^\circ$.
    \end{itemize}
\end{itemize}
Lưu ý: Do bánh răng bằng nhựa, tránh ép trục khi không cấp điện hoặc tải vượt quá lực kéo.

\subsection{Còi SFM-27}

\subsubsection{Giới thiệu nhanh}
SFM-27 là còi báo động chủ động (Active Buzzer) sử dụng công nghệ thạch anh, có hai tai bắt vít hai bên để cố định lên vỏ. Hoạt động trong dải 3 V -- 24 V, thích hợp cho mạch 5 V lẫn hệ thống 12 V/24 V.

\begin{figure}[H]
\centering
\includegraphics[width=0.45\linewidth]{Hinh_ve/buzzer_sfm27.png}
\caption{Còi SFM-27 (Active Buzzer)}
\label{fig:buzzer_sfm27}
\end{figure}

\subsubsection{Chức năng \& Ứng dụng}
Mục đích chính là phát âm thanh cảnh báo cường độ cao.
\begin{itemize}
    \item \textbf{Chức năng:} Phát tiếng "bíp" liên tục hoặc ngắt quãng khi có nguồn DC.
    \item \textbf{Ứng dụng:}
    \begin{itemize}
        \item Hệ thống báo động chống trộm, báo cháy.
        \item Cảnh báo lùi cho xe điện, robot.
        \item Thông báo trạng thái trên bảng điều khiển máy công nghiệp.
        \item Còi báo cho thiết bị đo lường.
    \end{itemize}
\end{itemize}

\subsubsection{Thông số kỹ thuật chính}
\begin{tabular}{lp{8.5cm}}
\textbf{Loại còi} & Còi chủ động (Active Buzzer) \\
\textbf{Điện áp hoạt động} & 3 V -- 24 V DC \\
\textbf{Dòng điện định mức} & $\le$ 15 mA (tại 12 V) \\
\textbf{Cường độ âm thanh} & $\ge$ 90 dB (ở 10 cm, tại 12 V) \\
\textbf{Tần số cộng hưởng} & $3000 \pm 500$ Hz \\
\textbf{Nhiệt độ hoạt động} & -20$^\circ$C đến +60$^\circ$C \\
\textbf{Kích thước} & Đường kính thân 30 mm; khoảng cách 2 lỗ bắt vít 40 mm
\end{tabular}

\subsubsection{Sơ đồ chân (Wiring)}
\begin{itemize}
    \item Dây Đỏ (+): Nối vào VCC (3V--24V).
    \item Dây Đen (-): Nối vào GND.
\end{itemize}
Ghi chú: Là còi chủ động nên chỉ cần cấp nguồn là hoạt động; để điều khiển bằng Arduino/ESP32 nên dùng transistor (ví dụ S8050) làm công tắc để tránh kéo quá tải chân IO.

\subsubsection{Nguyên lý hoạt động}
SFM-27 tích hợp mạch dao động và màng rung thạch anh (piezo) trong vỏ. Khi cấp điện, mạch tạo dao động xoay chiều cỡ ~3000 Hz đưa vào màng thạch anh, tạo ra rung động và phát âm.

\subsection{Module ADC ADS1115 16-bit 4 kênh}

\subsubsection{Giới thiệu nhanh}
ADS1115 là module ADC 16-bit, 4 kênh, giao tiếp I2C, tích hợp PGA cho phép khuếch đại tín hiệu yếu. Thích hợp cho đo lường chính xác mà ADC tích hợp vi điều khiển khó đạt được.
\begin{figure}[H]
\centering
\includegraphics[width=0.45\linewidth]{Hinh_ve/ads1115_module.png}
\caption{Module ADC ADS1115 16-bit 4 kênh (I2C)}
\label{fig:ads1115_module}
\end{figure}

\subsubsection{Chức năng \& Ứng dụng}
\begin{itemize}[label=\textbullet]
    \item \textbf{Chức năng:} Chuyển đổi analog sang digital 16-bit; hỗ trợ đo đơn (single-ended) hoặc vi sai (differential).
    \item \textbf{Ứng dụng:} Đo loadcell, cảm biến nhiệt độ chính xác, giám sát điện áp pin, hệ thống Data Acquisition.
\end{itemize}

\subsubsection{Thông số kỹ thuật chính}
\begin{tabular}{lp{8.5cm}}
\textbf{Độ phân giải} & 16 bits \\
\textbf{Giao tiếp} & I2C (hỗ trợ 4 địa chỉ khác nhau qua chân ADDR) \\
\textbf{Điện áp hoạt động} & 2.0 V -- 5.5 V \\
\textbf{Dòng tiêu thụ} & Rất thấp (chế độ liên tục ~150 $\mu$A) \\
\textbf{Tốc độ lấy mẫu} & 8 SPS -- 860 SPS (tuỳ cấu hình) \\
\textbf{PGA} & Lên đến x16 \\
\textbf{Số kênh} & 4 kênh đơn (A0--A3) hoặc 2 kênh vi sai
\end{tabular}

\subsubsection{Sơ đồ chân (Pinout)}
\begin{itemize}[label=\textbullet]
    \item VDD: Nguồn (2.0V--5.5V).\\
    \item GND: Mass.\\
    \item SCL / SDA: Đường I2C.\\
    \item ADDR: Chọn địa chỉ I2C (GND/VDD/SDA/SCL).\\
    \item ALRT: Ngắt/Comparator output (tùy dùng).\\
    \item A0--A3: 4 đầu vào analog.
\end{itemize}

\subsubsection{Nguyên lý hoạt động}
ADS1115 hoạt động dựa trên cấu trúc Delta-Sigma ($\Delta\Sigma$) ADC. Logic vận hành của nó diễn ra như sau:

\begin{enumerate}
    \item \textbf{Thu nhận \& Khuếch đại:} Tín hiệu Analog đi vào qua các chân A0--A3. Nếu tín hiệu quá yếu, bộ PGA (Programmable Gain Amplifier) bên trong sẽ khuếch đại nó lên (lên tới 16 lần) để đảm bảo độ chính xác.
    \item \textbf{Chuyển đổi:} Bộ chuyển đổi Delta-Sigma lấy mẫu tín hiệu liên tục ở tần số cao, sử dụng các bộ lọc kỹ thuật số để triệt tiêu nhiễu tần số thấp và nhiễu nguồn (như nhiễu 50 Hz/60 Hz từ điện lưới).
    \item \textbf{Số hóa:} Tín hiệu được chuyển thành giá trị số 16-bit. Với dải đo mặc định 6.144 V, mỗi đơn vị (LSB) tương đương với khoảng $187.5\ \mu V$ -- một con số cực kỳ ấn tượng!
    \item \textbf{Truyền tải:} Kết quả cuối cùng được lưu vào các thanh ghi nội bộ. Vi điều khiển (như ESP32 hoặc Arduino) sẽ gọi dữ liệu này thông qua giao thức I2C chỉ với 2 dây tín hiệu.
\end{enumerate}

Mẹo nhỏ: Nếu bạn cần dùng nhiều module ADS1115 trên cùng một bo mạch, hãy tận dụng chân ADDR. Bằng cách nối chân này với các chân khác nhau, bạn có thể thiết lập tối đa 4 địa chỉ I2C khác nhau, cho phép đọc tới 16 kênh Analog chỉ trên một đường bus duy nhất!


\subsection{Module YX850 mạch tự động chuyển nguồn dự phòng 5-48VDC}
YX850 là một mạch tự động chuyển nguồn dự phòng được sử dụng để chuyển giữa hai nguồn chính và phụ (thường là nguồn pin, ắc quy) tự động để cấp cho hệ thống khi nguồn chính không còn hoạt động. Mạch sử dụng điện áp 5-48VDC, thích hợp cho các ứng dụng cần nguồn pin dự phòng.

\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{Hinh_ve/yx850.png}
\caption{Module YX850 thực tế}
\label{fig:yx850_module_real}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.9\linewidth]{Hinh_ve/YX850_2.png}
\caption{Nguyên lý chuyển nguồn chính và nguồn dự phòng của YX850}
\label{fig:yx850_principle}
\end{figure}

Mạch được thiết kế nhỏ gọn với cách sử dụng đơn giản nhưng vẫn đạt năng suất hiệu quả. Có thể sử dụng để cấp nguồn dự phòng cho hệ thống camera giám sát, hệ thống cửa ra vào kiểm soát, hệ thống báo động, hệ thống báo trộm.

Thông số kỹ thuật:
\begin{itemize}[label=\textbullet]
    \item Điện áp: 5-48VDC.
    \item Khi có nguồn, nó được kết nối với pin để sạc.
    \item Tải: 10A.
    \item Kích thước sản phẩm: 61x30mm.
\end{itemize}

\subsection{Module hạ áp LM2596}
LM2596 là IC ổn áp hạ áp (Buck Converter) dòng cao 3A phổ biến, chuyển đổi điện áp DC cao xuống mức thấp hơn (1.23V-30V) với hiệu suất lên tới 92\%. Nó có phiên bản cố định (3.3V, 5V, 12V) và điều chỉnh được (ADJ), thường dùng trong các module nguồn nhỏ gọn cho dự án robot, camera và IoT.

\begin{figure}[H]
\centering
\includegraphics[width=0.55\linewidth]{Hinh_ve/LM2596S.png}
\caption{Module hạ áp LM2596}
\label{fig:lm2596_module}
\end{figure}

Vì nguồn dự phòng là pin 3,7V đấu nối tiếp và vì mạch cần 5V, nên cần module hạ áp để điện áp ở ngưỡng 5V cho hệ thống sử dụng ổn định.

Thông số kỹ thuật chính của module LM2596:
\begin{itemize}[label=\textbullet]
    \item Điện áp đầu vào: 3V-30V/35V.
    \item Điện áp đầu ra: Điều chỉnh được từ 1.5V-30V/35V (phiên bản ADJ).
    \item Dòng điện đầu ra: 3A (liên tục), có thể cần tản nhiệt nếu dòng cao hơn 1A.
    \item Hiệu suất: Lên tới 92\%.
    \item Tần số chuyển mạch: 150kHz (cho phép dùng linh kiện lọc nhỏ gọn). [1, 2, 3, 4, 5, 6]
\end{itemize}

Các đặc điểm nổi bật:
\begin{itemize}[label=\textbullet]
    \item Thiết kế đơn giản: Sử dụng rất ít linh kiện ngoại vi.
    \item Bảo vệ tích hợp: Có chức năng bảo vệ quá nhiệt và ngắn mạch.
    \item Ứng dụng: Mạch sạc pin, cấp nguồn cho vi điều khiển, camera, motor, loa mini.
\end{itemize}

\subsection{Adapter 5V 2A}
Chuyển đổi nguồn:
\begin{figure}[H]
\centering
\includegraphics[width=0.5\linewidth]{Hinh_ve/adapter_5v_2a.png}
\caption{Adapter nguồn 5V 2A}
\label{fig:adapter_5v_2a}
\end{figure}

\begin{itemize}[label=\textbullet]
    \item Biến đổi AC 220V (điện lưới) sang DC 5V.
    \item Đây là bộ nguồn AC-DC switching.
\end{itemize}

Cung cấp năng lượng ổn định:
\begin{itemize}[label=\textbullet]
    \item Cấp điện cho:
    \begin{itemize}
        \item Vi điều khiển (ESP32, Arduino).
        \item Sensor (MQ135).
        \item Module (Relay, WiFi).
    \end{itemize}
\end{itemize}

Thông số kỹ thuật quan trọng:

Thông số đầu vào (Input):
\begin{itemize}
    \item Điện áp: 100-240V AC.
    \item Tần số: 50-60Hz.
\end{itemize}

Thông số đầu ra (Output):
\begin{itemize}[label=\textbullet]
    \item Điện áp: 5V DC.
    \item Dòng tối đa: 2A.
\end{itemize}


\subsection{Nguồn dự phòng pin lithium 18650}
Pin 18650 là loại pin sạc Lithium-ion (Li-ion) hình trụ, được sử dụng phổ biến trong các hệ thống nhúng và IoT.

\begin{figure}[H]
\centering
\includegraphics[width=0.5\linewidth]{Hinh_ve/pin_lithium_18650.png}
\caption{Pin lithium-ion 18650 dùng làm nguồn dự phòng}
\label{fig:pin_18650}
\end{figure}

\begin{itemize}[label=\textbullet]
    \item Ký hiệu 18650:
    \begin{itemize}
        \item Đường kính: 18 mm.
        \item Chiều dài: 65 mm.
    \end{itemize}
\end{itemize}

Chức năng trong hệ thống:

Trong hệ thống, pin 18650 đóng vai trò:
\begin{itemize}
    \item Nguồn dự phòng (backup power) khi mất điện từ adapter.
    \item Duy trì hoạt động cho:
    \begin{itemize}
        \item ESP32.
        \item Cảm biến MQ-135.
        \item Relay và quạt.
    \end{itemize}
\end{itemize}

Đảm bảo hệ thống giám sát không bị gián đoạn.

Thông số kỹ thuật chính:
\begin{itemize}
    \item Điện áp: 3.7V.
    \item Dung lượng: xấp xỉ 1500 mAh (tùy loại thực tế).
\end{itemize}

% Bảng 2.1: Dùng môi trường table chuẩn để vào danh mục bảng biểu
\subsection{Bảng cấu hình chân GPIO}
Bảng dưới đây tổng hợp toàn bộ các chân GPIO và các linh kiện được kết nối trong hệ thống:
\begin{table}[H]
\centering
\caption{Cấu hình chân GPIO của ESP32 trong hệ thống}
\label{tab:esp32_gpio_config}
\begin{tabular}{|l|l|l|l|}
\hline
\textbf{Chức năng} & \textbf{Chân} & \textbf{Loại} & \textbf{Linh kiện} \\
\hline
Khí phòng 1 & A0 & I2C ADC & MQ-135 \#1 \\
Khí phòng 1 & GND & GND & GND \\
Khí phòng 2 & A1 & I2C ADC & MQ-135 \#2 \\
Khí phòng 2 & GND & GND & GND \\
Relay 1 & GPIO 32 & Output & Relay IN1 \\
Relay 2 & GPIO 33 & Output & Relay IN2 \\
I2C SDA & GPIO 21 & I2C SDA & LCD, ADS1115 \\
I2C SCL & GPIO 22 & I2C SCL & LCD, ADS1115 \\
Servo phòng 1 & GPIO 26 & PWM & SG90 \#1 \\
Servo phòng 2 & GPIO 27 & PWM & SG90 \#2 \\
Buzzer phòng 1 & GPIO 25 & Output & SFM-27 \#1 \\
Buzzer phòng 2 & GPIO 4 & Output & SFM-27 \#2 \\
Nút khẩn cấp 1 & GPIO 34 & Input ISR & EMG R1 \\
Nút khẩn cấp 2 & GPIO 35 & Input ISR & EMG R2 \\
Nút khẩn cấp chung & GPIO 23 & Input ISR & EMG ALL \\
Nguồn chính & 5V/GND & Power & Adapter 5V 2A \\
Nguồn 3.3V & 3V3 & Power & CP2102/CH340 \\
\hline
\end{tabular}
\end{table}

\section{Phần mềm của hệ thống}
\subsection{Tổng quan chức năng phần mềm}
Hệ thống phần mềm trên ESP32 thực hiện các chức năng:
\begin{itemize}[label=\textbullet]
    \item \textbf{Khởi động hệ thống:} Đồng bộ thời gian từ server NTP (\texttt{pool.ntp.org}, \texttt{time.nist.gov}, múi giờ GMT+7) để đảm bảo timestamp dữ liệu telemetry chính xác cho việc lưu trữ lịch sử và vẽ biểu đồ.
    
    \item \textbf{Đọc dữ liệu từ 2 cảm biến MQ-135 qua ADS1115:} Đọc ADC 16-bit từ ADS1115 (kênh A0, A1), sau đó ánh xạ về thang 12-bit (0-4095) để giữ tương thích với các ngưỡng AQI đã hiệu chỉnh.
    
    \item \textbf{Lọc nhiễu bằng EWMA:} Áp dụng bộ lọc Exponential Weighted Moving Average (hệ số $\alpha = 0.2$) để làm mượt dao động ngẫu nhiên của ADC do nhiễu nguồn điện và sự biến đổi nhanh của cảm biến, tránh kích hoạt relay bất thình lình khi giá trị dao động quanh ngưỡng.
    
    \item \textbf{Phát hiện lỗi cảm biến:} Kiểm tra xem giá trị lọc có nằm trong dải hợp lệ $[\texttt{MQ\_MIN}, \texttt{MQ\_MAX}] = [50, 3800]$. Nếu vượt ra ngoài dải, đánh dấu trường \texttt{error = true} và gắn \texttt{"sensor": "ERR"} vào payload MQTT để thông báo cho backend biết thiết bị cảm biến gặp vấn đề.
    
    \item \textbf{Phân loại chất lượng không khí:} Dựa trên giá trị lọc, phân loại mức độ ô nhiễm thành 4 cấp: GOOD, MOD (Moderate), BAD, DANG (Dangerous).
    
    \item \textbf{Gửi dữ liệu lên HiveMQ Broker:} Định kỳ (mỗi 3 giây) gửi gói tin dữ liệu định dạng JSON chứa các thông tin: nồng độ khí, mức ô nhiễm, trạng thái quạt, chế độ hoạt động, trạng thái lỗi thông qua giao thức MQTT qua kết nối TLS an toàn (port 8883).
    
    \item \textbf{Nhận lệnh điều khiển từ server:} Subscribe vào topic MQTT để lắng nghe các lệnh từ người dùng hoặc hệ thống điều khiển từ xa, bao gồm:
    \begin{itemize}
        \item Lệnh chuyển chế độ: AUTO hoặc MANUAL.
        \item Lệnh điều khiển quạt, còi và góc mở cửa sổ: \texttt{fan}, \texttt{buzzer}, \texttt{window}.
    \end{itemize}
    
    \item \textbf{Điều khiển quạt tương ứng từng phòng:}
    \begin{itemize}[label=--]
        \item Chế độ AUTO: Hệ thống tự động kích hoạt relay để bật quạt khi nồng độ vượt ngưỡng, tắt khi nồng độ hạ xuống.
        \item Chế độ MANUAL: Lệnh từ người dùng được ưu tiên cao nhất, ghi đè lên logic tự động.
    \end{itemize}
    
    \item \textbf{Hiển thị trên LCD I2C 16×2:} Xoay vòng hiển thị thông tin các phòng mỗi 2 giây (paging), bao gồm: nồng độ khí, mức cảnh báo, trạng thái quạt, chế độ điều khiển.
    
    \item \textbf{Cơ chế chống nhiễu I2C khi Relay đóng/ngắt:} Khi có sự kiện chuyển trạng thái quạt (relay đóng/ngắt), dòng cảm ứng ngược từ cuộn relay có thể gây nhiễu lan ra bus I2C, khiến LCD bị treo. Để xử lý, task LCD sẽ re-initialize I2C bus (\texttt{Wire.begin()}) và LCD khi phát hiện cờ \texttt{flagResetI2C}, bảo đảm giao tiếp I2C được khôi phục.
    \item \textbf{Xử lý khẩn cấp tại hiện trường:} Hệ thống có 3 nút bấm vật lý để bật/tắt trạng thái EMERGENCY theo từng phòng hoặc toàn hệ thống, được xử lý bằng ngắt ngoài và task ưu tiên cao nhất.
\end{itemize}



\subsection{Phân ngưỡng chất lượng không khí (AQI Classification)}
Hệ thống phân loại mức độ ô nhiễm dựa trên giá trị ADC 12-bit (0–4095) đã lọc từ cảm biến MQ-135. Các ngưỡng được chọn dựa trên khảo sát thực nghiệm trong môi trường làm việc:

% Bảng 2.2: Dùng môi trường table chuẩn để vào danh mục bảng biểu
\begin{table}[H]
\centering
\caption{Bảng phân ngưỡng chất lượng không khí (AQI) và hành động điều khiển}
\label{tab:aqi_classification}
\begin{tabular}{|c|c|c|c|}
\hline
\textbf{Giá trị raw ADC} & \textbf{Mức} & \textbf{Ý nghĩa} & \textbf{Hành động AUTO} \\
\hline
$< 1200$ & GOOD & Tốt & Tắt quạt \\
1200 -- 1999 & MOD & Trung bình & Tắt quạt \\
2000 -- 2999 & BAD & Kém & Bật quạt \\
$\geq 3000$ & DANG & Nguy hiểm & Bật quạt \\
\hline
\end{tabular}
\end{table}

\textbf{Lưu ý quan trọng:} Giá trị này là raw ADC 12-bit chứ chưa được quy đổi sang ppm (parts per million). Việc quy đổi MQ-135 sang ppm yêu cầu Calibration đường cong $R_0/R_s$ phức tạp và vượt quá phạm vi của bài tập lớn này. Nhóm sử dụng raw ADC làm chỉ số tương đối để so sánh ngưỡng và xác định tình trạng ô nhiễm tại vị trí đặt cảm biến.

\subsection{Bộ lọc EWMA (Exponential Weighted Moving Average) và Phát hiện lỗi cảm biến}

\subsubsection{Bộ lọc EWMA}
Trong thực tế, giá trị ADC của cảm biến thường dao động do nhiễu từ:
\begin{itemize}[label=\textbullet]
    \item Nguồn điện 5V dùng chung với motor/relay (dòng cảm ứng, sụt áp tức thời).
    \item Sự nhạy cảm tự nhiên của cảm biến bán dẫn với biến thiên nhiệt độ, độ ẩm.
    \item Bản chất của ADC khi lấy mẫu với SNR (Signal-to-Noise Ratio) hạn chế.
\end{itemize}

Để xử lý, hệ thống áp dụng bộ lọc Exponential Weighted Moving Average (EWMA) với công thức:

$$\text{filtered\_value}_n = \alpha \cdot \text{raw\_value}_n + (1 - \alpha) \cdot \text{filtered\_value}_{n-1}$$

Trong đó:
\begin{itemize}[label=\textbullet]
    \item $\alpha = 0.2$ (hệ số làm mượt, chọn thủ công qua thử nghiệm).
    \item $\text{raw\_value}_n$: giá trị ADC vừa đọc.
    \item $\text{filtered\_value}_{n-1}$: giá trị lọc từ lần đọc trước.
\end{itemize}

Mục đích:
\begin{itemize}[label=\textbullet]
    \item Làm mượt các dao động ngẫu nhiên, làm cho quyết định kích hoạt relay ổn định hơn.
    \item Tránh hiện tượng quạt bị "trip" (bật/tắt liên tục) khi giá trị dao động quanh ngưỡng.
    \item Giữ tính responsive của hệ thống (vẫn phản hồi nhanh với những thay đổi lớn trong nồng độ khí).
\end{itemize}

\subsubsection{Phát hiện lỗi cảm biến}
Cảm biến MQ-135 có thể gặp lỗi do:
\begin{itemize}[label=\textbullet]
    \item Tấm nhiệt bị hỏng hoặc không sấy đủ (khi vừa khởi động).
    \item Chân A0 bị ngắn/mở.
    \item Độ ẩm hoặc hóa chất khắc nghiệt tấn công linh kiện.
\end{itemize}

Cơ chế phát hiện:
\begin{itemize}[label=\textbullet]
    \item Sau khi lọc EWMA, giá trị được kiểm tra xem có nằm trong dải hợp lệ: $\text{MQ\_MIN} = 50, \text{MQ\_MAX} = 3800$.
    \item Nếu giá trị $< 50$ hoặc $> 3800$, cảm biến được đánh dấu có lỗi (\texttt{error = true}).
    \item Khi có lỗi, payload MQTT sẽ gắn \texttt{"sensor": "ERR"} thay vì giá trị ADC, cho phép backend phát hiện và ghi log sự cố này.
    \item Quạt sẽ KHÔNG tự động bật khi cảm biến lỗi (chế độ AUTO) để tránh hành động bất thường; người dùng có thể điều khiển thủ công nếu cần.
\end{itemize}

\subsection{Cơ chế chống nhiễu I2C khi Relay đóng/ngắt}
Vấn đề thực tế gặp phải:
\begin{itemize}[label=\textbullet]
    \item Khi relay đóng/ngắt cuộn cảm của quạt, dòng điện chợt thay đổi (đặc biệt với quạt chạy bằng DC motor).
    \item Hiệu ứng cảm ứng ngược (back-EMF) tạo ra xung áp cao, lan truyền qua các dây dẫn xung quanh bus I2C.
    \item Bus I2C là bus kéo lên (pull-up) với tần số cao (100 kHz ở Standard mode, 400 kHz ở Fast mode), rất nhạy cảm với nhiễu.
    \item Kết quả: LCD bị "treo" (không phản hồi), hiển thị ký tự rác hoặc mất kết nối I2C.
\end{itemize}

Giải pháp triển khai:
\begin{itemize}[label=\textbullet]
    \item Trong hàm \texttt{executeFanControl()}, ngay sau khi gọi \texttt{digitalWrite()} để thay đổi trạng thái relay, đặt cờ \texttt{flagResetI2C = true}.
    \item Task LCD (chạy với chu kỳ 2 giây) kiểm tra cờ này. Nếu điều kiện là true, task sẽ thực hiện:
    \begin{enumerate}[label=\arabic*.]
        \item Gọi \texttt{Wire.end()} để ngắt kết nối I2C.
        \item Gọi \texttt{Wire.begin(SDA\textunderscore PIN, SCL\textunderscore PIN)} để khởi tạo lại I2C bus.
        \item Gọi \texttt{lcd.init()} và \texttt{lcd.backlight()} để re-initialize LCD.
        \item Đặt \texttt{flagResetI2C = false}.
    \end{enumerate}
    \item Quá trình này mất khoảng 10–50 ms, không ảnh hưởng đáng kể đến tốc độ refresh trên LCD.
\end{itemize}

Đây là một điểm quan trọng chứng minh nhóm đã thử nghiệm thực tế và xử lý các lỗi kỹ thuật gặp phải trong quá trình lắp ráp hệ thống.

\subsection{Đồng bộ thời gian NTP}
Hệ thống cần timestamp chính xác cho các mục đích:
\begin{itemize}[label=\textbullet]
    \item Gắn nhãn thời gian vào mỗi dữ liệu telemetry gửi lên server (để tạo biểu đồ lịch sử chính xác).
    \item Ghi log các sự kiện (kích hoạt alarm, thay đổi chế độ, lỗi).
    \item Đồng bộ với timezone của khu vực hoạt động (GMT+7 cho Việt Nam).
\end{itemize}

Cách triển khai:
\begin{itemize}[label=\textbullet]
    \item Trong hàm \texttt{setup()}, ESP32 gọi:
    \begin{itemize}[label=--]
        \item \texttt{configTime(7*3600, 0, "pool.ntp.org", "time.nist.gov");}
        \item Tham số đầu tiên là offset múi giờ (7 giờ = GMT+7).
        \item Tham số 0 là offset daylight saving (không áp dụng cho Việt Nam).
        \item Hai tham số cuối là NTP server backup.
    \end{itemize}
    \item ESP32 sẽ đợi tối đa 10 giây để nhận phản hồi từ NTP server.
    \item Nếu thành công, giờ hệ thống sẽ được đặt. Nếu thất bại, ESP32 vẫn hoạt động nhưng timestamp sẽ không chính xác.
    \item Để lấy giờ hiện tại trong chương trình, sử dụng:
    \begin{itemize}[label=--]
        \item \texttt{time\_t now = time(nullptr);} hoặc
        \item \texttt{struct tm timeinfo; localtime\_r(\&now, \&timeinfo);}
    \end{itemize}
\end{itemize}

\subsection{Giao thức truyền tin MQTT}

\subsubsection{Cấu hình Broker MQTT}
\begin{itemize}[label=\textbullet]
    \item \textbf{Server:} HiveMQ Cloud (\texttt{21b69e31ed5e...hivemq.cloud}).
    \item \textbf{Port:} 8883 (MQTT over TLS/SSL, kết nối an toàn).
    \item \textbf{Xác thực:} Username và Password cấu hình trong firmware. Lưu ý: mật khẩu hiển thị trong mã - cần che dấu thực tế.
    \item \textbf{Client ID:} \texttt{ESP32\_<MAC\_ADDRESS>} để đảm bảo duy nhất.
    \item \textbf{Keep-Alive:} 60 giây.
    \item \textbf{Buffer size:} 1024 byte cho publish/subscribe payload.
\end{itemize}

\subsubsection{Bảo mật TLS}
\begin{itemize}[label=\textbullet]
    \item Sử dụng \texttt{WiFiClientSecure} thay vì \texttt{WiFiClient}.
    \item Gọi \texttt{client.setInsecure();} để bỏ qua xác thực CA (bộ nhớ ESP32 giới hạn).
    \item Phương pháp này giảm bớt bảo mật nhưng vẫn mã hóa dữ liệu trên đường truyền.
    \item Sản phẩm thực tế nên tích hợp certificate CA để xác thực server.
\end{itemize}

\subsubsection{Cấu trúc Topic MQTT}
Hệ thống sử dụng hai bộ topic: chuẩn mới (với device ID) và legacy (để tương thích):

\begin{table}[htbp]
\centering
\caption{Cấu trúc Topic MQTT}
\label{tab:mqtt_topics}
\small
\begin{tabular}{|l|l|c|l|}
\hline
\textbf{Topic} & \textbf{Hướng} & QoS & \textbf{Mô tả} \\
\hline
\texttt{air/data/\{id\}} & ESP32 $\rightarrow$ Backend & 0 & JSON telemetry \\
\hline
\texttt{air/data} & ESP32 $\rightarrow$ Backend & 0 & Legacy telemetry \\
\hline
\texttt{air/control/\{id\}} & Backend $\rightarrow$ ESP32 & 1 & JSON command \\
\hline
\texttt{air/control} & Backend $\rightarrow$ ESP32 & 1 & Legacy command \\
\hline
\texttt{air/updatefirmware} & Backend $\rightarrow$ ESP32 & 1 & OTA (URL+ver) \\
\hline
\texttt{air/firmwareupdatestatus} & ESP32 $\rightarrow$ Backend & 0 & OTA status \\
\hline
\end{tabular}
\end{table}

\textbf{Payload Telemetry} (ESP32 publish lên, mỗi 3 giây, QoS 0):
\begin{lstlisting}[language={}]
{
  "rooms": [
    {
            "id": 1,
            "value": 1450,
            "level": "MOD",
            "emergency": false,
      "mode": "AUTO",
            "fan": false,
            "buzzer": false,
            "window": 45,
            "sensor": "OK"
    },
    {
            "id": 2,
            "value": 2150,
            "level": "BAD",
            "emergency": false,
      "mode": "MANUAL",
            "fan": true,
            "buzzer": false,
            "window": 90,
      "sensor": "OK"
    }
  ]
}
\end{lstlisting}

\textbf{Payload Control} (Backend gửi xuống, QoS 1):
\begin{lstlisting}[language={}]
{
  "room": 1,
    "mode": "MANUAL",
    "fan": false,
    "buzzer": false,
    "window": 90
}
\end{lstlisting}

Lưu ý: Payload control gửi đủ trường bắt buộc \texttt{room}, \texttt{mode}; các trường \texttt{fan}, \texttt{buzzer}, \texttt{window} là tùy chọn và chỉ có hiệu lực khi \texttt{mode = MANUAL}.

\subsection{Thiết kế không sử dụng RTOS (mô hình đối chiếu)}
Phần này trình bày kiến trúc lý thuyết để đối chiếu. Phiên bản triển khai thực tế của hệ thống sử dụng FreeRTOS, được mô tả trong mục kế tiếp.


\subsubsection{Phân chia Task (Logical Tasks)}
Hệ thống được chia thành các tác vụ logic:

\begin{table}[htbp]
\centering
\caption{Phân chia tác vụ logic khi không sử dụng RTOS}
\label{tab:logical_tasks_no_rtos}
\begin{tabular}{|l|l|}
\hline
\textbf{Task} & \textbf{Chức năng} \\
\hline
Sensor Task & Đọc dữ liệu 2 MQ-135 \\
Processing Task & Xử lý, phân loại mức khí \\
MQTT Task & Gửi dữ liệu lên broker \\
Control Task & Điều khiển quạt \\
Command Task & Nhận lệnh từ server \\
\hline
\end{tabular}
\end{table}

\subsubsection{Scheduling (Lập lịch)}
Sử dụng cooperative scheduling (vòng lặp loop) với millis():

\begin{lstlisting}[language=C]
void loop() {
    readSensorTask();
    processTask();
    mqttPublishTask();
    mqttReceiveTask();
    controlTask();
}
\end{lstlisting}

Cách triển khai:
\begin{itemize}[label=\textbullet]
    \item Mỗi task có timer riêng.
    \item Không dùng delay().
\end{itemize}

Ví dụ:
\begin{lstlisting}[language=C]
if (millis() - lastRead >= 1000) {
    readSensor();
}
\end{lstlisting}

\subsubsection{FSM - Máy trạng thái hữu hạn}
Mỗi phòng có FSM riêng.

Trạng thái chính:
\begin{itemize}[label=\textbullet]
    \item MODE:
    \begin{itemize}[label=--]
        \item AUTO.
        \item MANUAL.
    \end{itemize}
    \item FAN STATE:
    \begin{itemize}[label=--]
        \item OFF.
        \item ON.
    \end{itemize}
\end{itemize}

\subsubsection{Logic điều khiển}
Nguyên tắc xử lý Auto và Manual:

\begin{table}[htbp]
\centering
\caption{Nguyên tắc điều khiển Auto và Manual}
\label{tab:auto_manual_logic_no_rtos}
\begin{tabular}{|l|l|}
\hline
\textbf{Mode} & \textbf{Điều khiển} \\
\hline
AUTO & Hệ thống tự quyết theo ngưỡng \\
MANUAL & Người dùng điều khiển hoàn toàn \\
\hline
\end{tabular}
\end{table}

\subsubsection{Luồng xử lý lệnh từ server}
\textbf{Bước 1:} Nhận lệnh setup.

Ví dụ MQTT:
\begin{lstlisting}[language={}]
{
  "room": 1,
    "mode": "MANUAL"
}
\end{lstlisting}

ESP32:
\begin{itemize}[label=\textbullet]
    \item Cập nhật mode.
    \item Reset trạng thái điều khiển.
\end{itemize}

\textbf{Bước 2:} Nếu là MANUAL thì nhận lệnh điều khiển.

\begin{lstlisting}[language={}]
{
  "room": 1,
    "fan": true
}
\end{lstlisting}

\subsubsection{Pseudocode FSM}
\begin{lstlisting}[language=C]
if (room.mode == AUTO) {
    if (airQuality > threshold) {
        fan = ON;
    } else {
        fan = OFF;
    }
}

if (room.mode == MANUAL) {
    fan = userCommand;
}
\end{lstlisting}

\subsubsection{Xử lý ngoại lệ}
Vấn đề:
\begin{itemize}[label=\textbullet]
    \item Xung đột giữa Auto và Manual.
\end{itemize}

Giải pháp:
\begin{itemize}[label=\textbullet]
    \item Khi nhận lệnh chuyển mode:
    \begin{itemize}[label=--]
        \item Ưu tiên tuyệt đối MANUAL.
        \item Auto bị disable hoàn toàn.
    \end{itemize}
\end{itemize}

\begin{lstlisting}[language=C]
if (modeChangedToManual) {
    disableAutoControl();
}
\end{lstlisting}

\subsection{Thiết kế có sử dụng RTOS (FreeRTOS trên ESP32)}
\subsubsection{Phân chia Task (Real Tasks)}
Hệ thống sử dụng 6 task chạy song song trên FreeRTOS. Bảng dưới trình bày chi tiết từng task:

% Bảng 2.6: Dùng môi trường table chuẩn để vào danh mục bảng biểu
\begin{table}[H]
\centering
\caption{Phân chia tác vụ khi sử dụng RTOS trên ESP32}
\label{tab:real_tasks_rtos}
\begin{tabular}{|l|c|c|c|l|}
\hline
\textbf{Task} & \textbf{Stack} & \textbf{Pri} & \textbf{Chu kỳ} & \textbf{Chức năng chính} \\
\hline
taskEmergency & 3 KB & 6 & Sự kiện & ISR nút khẩn cấp, Emergency \\
taskOTA & 8 KB & 5 & Động & OTA, ghi flash \\
taskControl & 3 KB & 4 & 1.5 s & FSM: Emergency $>$ DANG $>$ MANUAL $>$ AUTO \\
taskSensor & 2 KB & 3 & 1 s & ADC, EWMA, phát hiện lỗi, AQI \\
taskLCD & 2 KB & 2 & 2 s & LCD paging, reset I2C \\
taskMQTT & 8 KB & 1 & 50 ms/3 s & WiFi/MQTT, control/telemetry \\
\hline
\end{tabular}
\end{table}

\textbf{Giải thích chi tiết:}
\begin{itemize}[label=\textbullet]
    \item \textbf{Stack Size:} \texttt{taskMQTT} và \texttt{taskOTA} dùng 8 KB do cần buffer lớn cho TLS/OTA. Các task còn lại tối ưu 2-3 KB.
    
    \item \textbf{Priority:} \texttt{taskEmergency} có ưu tiên cao nhất để đáp ứng an toàn tức thời. \texttt{taskOTA} cao hơn \texttt{taskControl} để tránh gián đoạn khi đang cập nhật firmware.
    
    \item \textbf{Period:} Các task vận hành theo chu kỳ riêng; riêng \texttt{taskEmergency} là event-driven (không polling), còn \texttt{taskOTA} chỉ tồn tại trong lúc cập nhật.
    
    \item \textbf{Chức năng:} Mô tả nhiệm vụ của mỗi task (nêu chi tiết ở các subsection sau).
\end{itemize}

\subsubsection{Kiến trúc Task}
Sensor Task/Emergency Task/MQTT Callback $\rightarrow$ Notify $\rightarrow$ Control Task

\hspace*{4em}$\downarrow$

\hspace*{2em}LCD Task (I2C) và MQTT Task (truyền thông)

\subsubsection{Quản lý tài nguyên}
\paragraph{Mutex}
\begin{itemize}[label=\textbullet]
    \item \texttt{dataMutex}: Bảo vệ dữ liệu trạng thái phòng (mode, fan, buzzer, window, level, emergency).
    \item \texttt{i2cMutex}: Bảo vệ bus I2C dùng chung LCD và ADS1115.
    \item \texttt{mqttMutex}: Bảo vệ thao tác publish MQTT khi nhiều task cùng cần gửi.
\end{itemize}

\begin{lstlisting}[language=C]
xSemaphoreTake(dataMutex, portMAX_DELAY);
// cap nhat rooms[]
xSemaphoreGive(dataMutex);
\end{lstlisting}

\paragraph{Task Notification}
\begin{itemize}[label=\textbullet]
    \item ISR và callback MQTT đánh thức trực tiếp \texttt{taskControl} bằng \texttt{xTaskNotifyGive()} thay cho polling.
\end{itemize}

\subsubsection{FSM trong RTOS}
FSM vẫn giữ nguyên logic nhưng chạy trong Control Task.

\subsubsection{Xử lý MQTT}
Khi nhận lệnh:
\begin{lstlisting}[language=C]
void mqttCallback(...) {
    parseCommand();
    xTaskNotifyGive(controlTaskHandle);
}
\end{lstlisting}

\subsubsection{Xử lý Auto và Manual (RTOS version)}
Nguyên tắc:
\begin{itemize}[label=\textbullet]
    \item Control Task xử lý theo thứ tự ưu tiên: EMERGENCY $>$ DANG-Lockout $>$ MANUAL $>$ AUTO.
    \item Khi phòng ở trạng thái EMERGENCY, hệ thống bỏ qua điều khiển từ xa để bảo đảm an toàn hiện trường.
\end{itemize}

\begin{lstlisting}[language=C]
if (room.isEmergency) {
    forceEmergencyActuators();
} else if (room.mode == MANUAL) {
    manualControl();
} else {
    autoControl();
}
\end{lstlisting}

\subsubsection{Xử lý ngoại lệ (chuẩn RTOS)}
Vấn đề:
\begin{itemize}[label=\textbullet]
    \item Race condition khi:
    \begin{itemize}[label=--]
        \item MQTT nhận lệnh.
        \item Control Task và Emergency Task đang xử lý đồng thời.
    \end{itemize}
\end{itemize}

Giải pháp:
\begin{itemize}[label=\textbullet]
    \item Dùng 3 mutex chuyên biệt và task notification.
    \item Không giữ đồng thời hai mutex để tránh deadlock.
\end{itemize}

\subsubsection{Ưu điểm RTOS}
\begin{itemize}[label=\textbullet]
    \item Task chạy song song.
    \item Không block.
    \item Dễ mở rộng nhiều phòng.
\end{itemize}

\subsection{Cơ chế điều khiển từ người dùng}
Flow chuẩn:
\begin{enumerate}[label=\arabic*.]
    \item User chọn phòng trên Dashboard.
    \item User chọn chế độ:
    \begin{itemize}[label=--]
        \item AUTO: Hệ thống tự động kích hoạt quạt theo ngưỡng nồng độ khí.
        \item MANUAL: User điều khiển quạt thủ công.
    \end{itemize}
    \item Backend gửi lệnh MQTT (payload đầy đủ):
\begin{lstlisting}[language={}]
{
  "room": 1,
  "mode": "MANUAL",
  "fan": false,
  "buzzer": false,
  "window": 90
}
\end{lstlisting}
    \item ESP32 nhận lệnh, cập nhật trạng thái của phòng 1.
    \item Payload gồm trường bắt buộc (\texttt{room}, \texttt{mode}) và trường tùy chọn (\texttt{fan}, \texttt{buzzer}, \texttt{window}) khi ở chế độ MANUAL.
    \item Nếu phòng đang ở trạng thái EMERGENCY, lệnh điều khiển từ xa bị từ chối để bảo đảm an toàn.
\end{enumerate}

\subsection{Hệ thống khẩn cấp (Emergency Mode)}

\subsubsection{Mục tiêu và cơ chế kích hoạt}
Hệ thống bổ sung cơ chế xử lý khẩn cấp bằng 3 nút bấm vật lý để người vận hành can thiệp ngay tại hiện trường:
\begin{itemize}[label=\textbullet]
    \item \texttt{BTN\_EMG\_R1} (GPIO 34): Toggle trạng thái khẩn cấp của phòng 1.
    \item \texttt{BTN\_EMG\_R2} (GPIO 35): Toggle trạng thái khẩn cấp của phòng 2.
    \item \texttt{BTN\_EMG\_ALL} (GPIO 23): Toggle đồng thời cả hai phòng.
\end{itemize}

\subsubsection{ISR và debounce}
Ba nút bấm được gắn ngắt cạnh xuống (FALLING). Trong ISR, firmware dùng debounce 250 ms, đặt cờ sự kiện và gọi \texttt{vTaskNotifyGiveFromISR()} để đánh thức \texttt{taskEmergency} ngay lập tức.

\subsubsection{Logic ưu tiên trong điều khiển}
Trong \texttt{taskControl}, thứ tự ưu tiên xử lý là:
\begin{enumerate}[label=\arabic*.]
    \item \textbf{EMERGENCY:} ép còi ON, quạt ON, cửa sổ mở 180 độ.
    \item \textbf{DANG-Lockout:} khi mức DANG ở AUTO, hệ thống ép về MANUAL an toàn cho tới khi người vận hành xác nhận lại.
    \item \textbf{MANUAL:} chấp hành lệnh người dùng từ Dashboard.
    \item \textbf{AUTO:} điều khiển theo ngưỡng AQI.
\end{enumerate}

\subsection{Watchdog Timer và khôi phục trạng thái}

\subsubsection{Task Watchdog}
Hệ thống bật Task WDT timeout 30 giây để giám sát tính sống của các task chính. Mỗi task gọi \texttt{esp\_task\_wdt\_reset()} theo chu kỳ để xác nhận vẫn hoạt động.

\subsubsection{Khôi phục trạng thái sau reset}
Các trạng thái quan trọng (mode, fan, buzzer, góc servo, emergency) được lưu trên RTC memory. Nếu reset do WDT/panic, hệ thống khôi phục lại trạng thái trước đó để tránh mất trạng thái an toàn.

\subsection{Cơ chế cập nhật firmware OTA}

\subsubsection{Luồng OTA}
Quy trình OTA: nhận lệnh, tải firmware, ghi flash, publish trạng thái, khởi động lại.

\subsubsection{Ràng buộc an toàn OTA}
Firmware từ chối OTA khi có phòng EMERGENCY; các task khác giảm tải khi ghi flash.

\section{Phương pháp thực hiện và kết quả}

\subsection{Phương pháp thực hiện}

Quá trình hiện thực hóa hệ thống được thực hiện qua các bước cụ thể sau:

\subsubsection{Triển khai phần cứng}

\begin{itemize}[label=\textbullet]
    \item \textbf{Lắp ráp mạch nguồn:} Adapter 5V 2A làm nguồn chính, YX850 quản lý nguồn dự phòng (pin 18650), LM2596 ổn định 5V.
    
    \item \textbf{Kết nối cảm biến và cơ cấu chấp hành:} 2 MQ-135 vào ADS1115 (A0, A1); Relay qua GPIO 32/33; Servo SG90, Buzzer SFM-27 cập nhật trạng thái.
    
    \item \textbf{Thi công mô hình:} Để đảm bảo tính linh hoạt trong quá trình phát triển và kiểm tra hệ thống giám sát không khí nhà máy, nhóm đã tiến hành lắp ráp mô hình theo dạng thực nghiệm (prototype) trên nền tảng breadboard và các module rời. Các bước thi công cụ thể như sau:
    
    \begin{enumerate}[label=\roman*.]
        \item \textbf{Bố trí linh kiện tổng thể:}
        \begin{itemize}[label=\textperiodcentered]
            \item \textbf{Khối điều khiển trung tâm:} Vi điều khiển ESP32 và 02 cảm biến MQ-135 được cắm trực tiếp trên Breadboard (testboard) lớn. Cách bố trí này giúp thu gọn diện tích và dễ dàng thay đổi sơ đồ nối dây khi cần thiết.
            
            \item \textbf{Khối chấp hành:} Module Relay 2 kênh được đặt tách biệt, kết nối qua dây jumper để điều khiển 02 quạt tản nhiệt 5V (mô phỏng hệ thống thông gió tại 2 khu vực khác nhau trong nhà máy).
            
            \item \textbf{Khối quản lý năng lượng:} Các module nguồn được sắp xếp theo trình tự luồng điện để tối ưu hóa không gian, bao gồm: Jack DC input, mạch chuyển nguồn dự phòng YX850, mạch hạ áp LM2596 và khay pin Lithium 18650.
        \end{itemize}
        
        \item \textbf{Quy trình kết nối và đi dây (Wiring):}
        \begin{itemize}[label=\textperiodcentered]
            \item \textbf{Kết nối cảm biến:} 02 cảm biến MQ-135 được cấp nguồn 5V và nối tín hiệu analog vào ADS1115; ADS1115 giao tiếp với ESP32 qua bus I2C (SDA=21, SCL=22).
            
            \item \textbf{Kết nối điều khiển quạt:} Các chân GPIO điều khiển từ ESP32 được dẫn tới đầu vào (Input) của module Relay. Quạt được nối vào tiếp điểm của Relay, sử dụng nguồn 5V trực tiếp từ hệ thống nguồn chung.
            
            \item \textbf{Thiết lập hệ thống nguồn nối tầng:}
            \begin{enumerate}[label=\arabic*.]
                \item Nguồn từ Adapter (qua Jack DC) và nguồn từ Pin 18650 cùng đi vào mạch YX850 để thực hiện chức năng tự động chuyển đổi khi mất điện.
                
                \item Đầu ra của YX850 được nối tới mạch hạ áp LM2596 để điều chỉnh điện áp về mức 5V ổn định.
                
                \item Nguồn 5V sau hạ áp được dẫn tới breadboard để nuôi ESP32, các cảm biến và cấp điện cho Relay/Quạt.
            \end{enumerate}
        \end{itemize}
        
        \item \textbf{Hoàn thiện thực nghiệm:}
        \begin{itemize}[label=\textperiodcentered]
            \item Toàn bộ hệ thống dây dẫn được sử dụng là dây Jumper (Đực-Cái và Đực-Đực), các mối nối được kiểm tra kỹ lưỡng để đảm bảo độ tiếp xúc tốt và không gây sụt áp cho cảm biến.
            
            \item Các module được cố định tạm thời trên mặt phẳng thực nghiệm, đảm bảo khoảng cách an toàn giữa khối nguồn và khối điều khiển để tránh nhiễu điện từ.
        \end{itemize}
    \end{enumerate}
\end{itemize}

\subsubsection{Phát triển phần mềm}

\begin{itemize}[label=\textbullet]
    \item \textbf{Cấu hình môi trường:} Lập trình trên nền tảng Arduino IDE cho ESP32, sử dụng các thư viện chuyên dụng:
    \begin{itemize}[label=--]
        \item \texttt{WiFi.h} và \texttt{WiFiClientSecure.h} cho kết nối WiFi an toàn.
        \item \texttt{PubSubClient.h} cho giao thức MQTT.
        \item \texttt{LiquidCrystal\_I2C.h} cho điều khiển LCD.
        \item \texttt{time.h} và \texttt{sys/time.h} cho đồng bộ NTP.
        \item \texttt{freertos/FreeRTOS.h} để sử dụng RTOS.
    \end{itemize}
    
    \item \textbf{Khởi tạo hệ thống:} Trong \texttt{setup()}:
    \begin{itemize}[label=--]
        \item Cấu hình các chân GPIO cho relay, servo, buzzer và các nút bấm khẩn cấp.
        \item Khởi tạo I2C bus (SDA=21, SCL=22), LCD 16×2 và ADS1115 dùng chung bus.
        \item Kết nối WiFi tới access point.
        \item Đồng bộ thời gian từ NTP (\texttt{pool.ntp.org}, \texttt{time.nist.gov}, GMT+7).
        \item Khởi tạo MQTT client với kết nối TLS an toàn (port 8883).
    \end{itemize}
    
    \item \textbf{Xây dựng logic điều khiển:}
    \begin{itemize}[label=--]
        \item Hiện thực hóa máy trạng thái hữu hạn (FSM) để quản lý các chế độ AUTO (tự động theo ngưỡng) và MANUAL (người dùng điều khiển).
        \item Thiết lập kết nối MQTT over TLS (WiFiClientSecure) để truyền nhận dữ liệu với HiveMQ Broker.
        \item Áp dụng bộ lọc EWMA ($\alpha = 0.2$) để làm mượt dữ liệu ADC từ cảm biến.
        \item Phát hiện lỗi cảm biến bằng cách kiểm tra dải hợp lệ [50, 3800].
        \item Phân loại mức độ ô nhiễm thành 4 cấp (GOOD, MOD, BAD, DANG).
    \end{itemize}
    
    \item \textbf{Phân chia Task (nếu dùng RTOS):}
    \begin{itemize}[label=--]
        \item 6 task: \texttt{taskEmergency}, \texttt{taskOTA} (tạo động), \texttt{taskControl}, \texttt{taskSensor}, \texttt{taskLCD}, \texttt{taskMQTT}.
        \item Sử dụng 3 mutex chuyên biệt: \texttt{dataMutex}, \texttt{i2cMutex}, \texttt{mqttMutex}.
        \item Sử dụng flag volatile \texttt{flagResetI2C} để đồng bộ việc khởi tạo lại bus I2C sau khi relay đóng/ngắt.
    \end{itemize}
    
    \item \textbf{Callback MQTT:} Xử lý lệnh từ server khi ESP32 nhận được thông điệp trên topic \texttt{air/control} hoặc \texttt{air/updatefirmware}. Payload điều khiển được parse ra các trường (\texttt{room, mode, fan, buzzer, window}).
\end{itemize}

\subsection{Kết quả đạt được}

\subsubsection{Sản phẩm thực tế}

\begin{itemize}[label=\textbullet]
    \item Hệ thống đã được lắp ráp hoàn chỉnh thành một thiết bị giám sát IoT nhỏ gọn.
\end{itemize}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\linewidth]{Hinh_ve/hinh_san_pham.png}
\caption{Hệ thống giám sát chất lượng không khí trong nhà máy}
\label{fig:hinh_san_pham}
\end{figure}

\subsubsection{Giao diện giám sát và điều khiển}

\begin{itemize}[label=\textbullet]
    \item Dữ liệu về nồng độ khí (PPM) từ hai phòng được cập nhật liên tục lên Dashboard của Web/Mobile App dưới dạng biểu đồ và chỉ số trực quan.
    
    \item Người dùng có thể theo dõi trạng thái quạt và chuyển đổi chế độ hoạt động trực tiếp trên giao diện.
\end{itemize}

\subsubsection{Thử nghiệm khả năng vận hành}

\begin{itemize}[label=\textbullet]
    \item \textbf{Chế độ Auto:} Khi mô phỏng nồng độ khí cao (sử dụng bật lửa hoặc dung dịch cồn), cảm biến MQ-135 phản hồi nhanh chóng, ESP32 tự động kích hoạt quạt khi nồng độ vượt mức ngưỡng quy định.
    
    \item \textbf{Chế độ Manual:} Lệnh điều khiển từ App được thực thi với độ trễ thấp (thông thường $< 1$ giây), đồng thời có quyền ưu tiên cao nhất, ghi đè lên logic tự động.
    
    \item \textbf{Nguồn dự phòng:} Khi ngắt nguồn Adapter, mạch YX850 tự động chuyển sang dùng pin 18650, đảm bảo hệ thống giám sát không bị gián đoạn.
\end{itemize}

\subsection{Đánh giá và nhận xét}

Dựa trên các tiêu chí tại, nhóm đưa ra các đánh giá sau:

\begin{itemize}[label=\textbullet]
    \item \textbf{Độ chính xác:} Cảm biến MQ-135 đáp ứng tốt việc phát hiện sự thay đổi chất lượng không khí trong bối cảnh nhà máy.
    
    \item \textbf{Tính thời gian thực:} Việc áp dụng cơ chế lập lịch (Scheduling) hoặc RTOS giúp hệ thống xử lý đồng thời việc đọc dữ liệu và phản hồi lệnh từ xa một cách mượt mà.
    
    \item \textbf{Tính ổn định:} Hệ thống hoạt động ổn định trong thời gian dài thử nghiệm, kết nối MQTT tin cậy nhờ khả năng xử lý của ESP32.
\end{itemize}

\end{document}
